---
phase: 01-foundation-lead-capture
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - index.html
autonomous: true

must_haves:
  truths:
    - "Landing page displays testimonials section with social proof"
    - "Landing page displays Calendly booking widget"
    - "Landing page is fully responsive on mobile (< 768px)"
  artifacts:
    - path: "index.html"
      provides: "Enhanced landing page with testimonials and calendar"
      contains: ["testimonials", "calendly"]
  key_links:
    - from: "index.html"
      to: "Calendly"
      via: "inline widget embed"
      pattern: "calendly-inline-widget"
---

<objective>
Add testimonials section and calendar booking integration to the landing page.

Purpose: Increase conversion with social proof (LAND-03) and enable direct appointment booking (LAND-04). Verify mobile responsiveness (LAND-02).
Output: Landing page with testimonials, Calendly widget, and verified mobile responsiveness.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-lead-capture/01-RESEARCH.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add testimonials section</name>
  <files>
    index.html
  </files>
  <action>
    Add a testimonials section to index.html after the features section and before the pricing section.

    Add the following CSS to the `<style>` section:

    ```css
    /* Testimonials Section */
    .testimonials {
        padding: 6rem 2rem;
        background: var(--background);
    }

    .testimonials-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
        margin-top: 3rem;
    }

    .testimonial-card {
        background: var(--white);
        border-radius: 24px;
        padding: 2rem;
        box-shadow: 0 10px 30px rgba(29, 53, 87, 0.08);
        transition: transform 0.3s ease;
    }

    .testimonial-card:hover {
        transform: translateY(-5px);
    }

    .testimonial-quote {
        font-size: 1.125rem;
        color: var(--secondary);
        line-height: 1.7;
        margin-bottom: 1.5rem;
        position: relative;
    }

    .testimonial-quote::before {
        content: '"';
        font-family: 'DM Serif Display', serif;
        font-size: 4rem;
        color: var(--primary);
        opacity: 0.2;
        position: absolute;
        top: -1.5rem;
        left: -0.5rem;
    }

    .testimonial-author {
        display: flex;
        align-items: center;
        gap: 1rem;
    }

    .testimonial-avatar {
        width: 56px;
        height: 56px;
        border-radius: 50%;
        background: linear-gradient(135deg, var(--primary), var(--accent));
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
        color: white;
        font-weight: 700;
    }

    .testimonial-info h4 {
        font-size: 1rem;
        color: var(--secondary);
        margin-bottom: 0.25rem;
    }

    .testimonial-info p {
        font-size: 0.875rem;
        color: var(--neutral);
    }

    .testimonial-rating {
        color: var(--accent);
        font-size: 1rem;
        margin-bottom: 1rem;
    }

    /* Social proof stats bar */
    .social-proof {
        display: flex;
        justify-content: center;
        gap: 4rem;
        flex-wrap: wrap;
        margin-top: 3rem;
        padding-top: 3rem;
        border-top: 1px solid rgba(29, 53, 87, 0.1);
    }

    .proof-item {
        text-align: center;
    }

    .proof-value {
        font-family: 'DM Serif Display', serif;
        font-size: 2.5rem;
        color: var(--primary);
        line-height: 1;
    }

    .proof-label {
        font-size: 0.875rem;
        color: var(--neutral);
        margin-top: 0.5rem;
    }

    @media (max-width: 768px) {
        .social-proof {
            gap: 2rem;
        }

        .proof-value {
            font-size: 2rem;
        }
    }
    ```

    Add the following HTML section after the features section (after `</section><!-- Features -->`) and before the pricing section:

    ```html
    <!-- Testimonials Section -->
    <section class="testimonials" id="temoignages">
        <div class="container">
            <div class="section-header">
                <span class="section-tag">Temoignages</span>
                <h2>Ils nous font confiance</h2>
                <p>Decouvrez ce que nos clients pensent de Clickresto</p>
            </div>
            <div class="testimonials-grid">
                <div class="testimonial-card">
                    <div class="testimonial-rating">★★★★★</div>
                    <p class="testimonial-quote">
                        Depuis qu'on utilise Clickresto, nos commandes a emporter ont augmente de 40%. L'application est intuitive et nos clients adorent le programme de fidelite.
                    </p>
                    <div class="testimonial-author">
                        <div class="testimonial-avatar">MC</div>
                        <div class="testimonial-info">
                            <h4>Marie Chevalier</h4>
                            <p>Le Petit Bistrot, Lyon</p>
                        </div>
                    </div>
                </div>
                <div class="testimonial-card">
                    <div class="testimonial-rating">★★★★★</div>
                    <p class="testimonial-quote">
                        Le rapport qualite-prix est imbattable. Pas de commission sur les ventes, une equipe reactive. Je recommande a tous les restaurateurs.
                    </p>
                    <div class="testimonial-author">
                        <div class="testimonial-avatar">PD</div>
                        <div class="testimonial-info">
                            <h4>Philippe Dubois</h4>
                            <p>Chez Philippe, Bordeaux</p>
                        </div>
                    </div>
                </div>
                <div class="testimonial-card">
                    <div class="testimonial-rating">★★★★★</div>
                    <p class="testimonial-quote">
                        La mise en place a ete rapide et simple. En 2 semaines notre app etait prete. Les notifications push nous permettent de remplir les creux.
                    </p>
                    <div class="testimonial-author">
                        <div class="testimonial-avatar">SL</div>
                        <div class="testimonial-info">
                            <h4>Sophie Laurent</h4>
                            <p>La Table de Sophie, Paris</p>
                        </div>
                    </div>
                </div>
            </div>
            <div class="social-proof">
                <div class="proof-item">
                    <div class="proof-value">150+</div>
                    <div class="proof-label">Restaurants partenaires</div>
                </div>
                <div class="proof-item">
                    <div class="proof-value">50k+</div>
                    <div class="proof-label">Commandes par mois</div>
                </div>
                <div class="proof-item">
                    <div class="proof-value">4.8/5</div>
                    <div class="proof-label">Note moyenne</div>
                </div>
            </div>
        </div>
    </section>
    ```

    Add "Temoignages" link to the navigation:
    In the `.nav-links` div, add a link to the testimonials section:
    ```html
    <a href="#temoignages">Temoignages</a>
    ```
    Place it between "Fonctionnalites" and "Tarifs" links.
  </action>
  <verify>
    Open index.html in browser, scroll to testimonials section, verify it displays correctly.
  </verify>
  <done>
    - Testimonials section added with 3 testimonials
    - Social proof stats bar (150+ restaurants, 50k+ orders, 4.8 rating)
    - Navigation link added
    - Responsive design with grid layout
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Calendly calendar booking</name>
  <files>
    index.html
  </files>
  <action>
    Add a calendar booking section with Calendly embed. This will be placed in the contact section as an alternative to the form.

    Add the following CSS to the `<style>` section:

    ```css
    /* Calendar booking section */
    .booking-section {
        margin-top: 3rem;
        padding-top: 3rem;
        border-top: 1px solid var(--gray-200);
    }

    .booking-section h3 {
        font-size: 1.5rem;
        color: var(--secondary);
        margin-bottom: 0.5rem;
        text-align: center;
    }

    .booking-section > p {
        color: var(--neutral);
        text-align: center;
        margin-bottom: 2rem;
    }

    .calendly-container {
        background: var(--white);
        border-radius: 16px;
        overflow: hidden;
        box-shadow: 0 4px 20px rgba(29, 53, 87, 0.08);
    }

    /* Calendly widget styling override */
    .calendly-inline-widget {
        min-width: 320px;
        height: 650px;
    }

    @media (max-width: 768px) {
        .calendly-inline-widget {
            height: 700px;
        }
    }

    /* Option divider */
    .contact-divider {
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 2rem 0;
        color: var(--neutral);
        font-size: 0.875rem;
    }

    .contact-divider::before,
    .contact-divider::after {
        content: '';
        flex: 1;
        height: 1px;
        background: var(--gray-200);
    }

    .contact-divider span {
        padding: 0 1rem;
    }
    ```

    Modify the contact section HTML structure. After the contact form div, add the calendar booking option:

    ```html
    <div class="contact-divider">
        <span>ou</span>
    </div>

    <div class="booking-section">
        <h3>Reservez directement un creneau</h3>
        <p>Choisissez un horaire qui vous convient pour une demo personnalisee</p>
        <div class="calendly-container">
            <!-- Calendly inline widget begin -->
            <div class="calendly-inline-widget"
                 data-url="https://calendly.com/clickresto/demo?hide_gdpr_banner=1&primary_color=e63946"
                 style="min-width:320px;height:650px;">
            </div>
            <!-- Calendly inline widget end -->
        </div>
    </div>
    ```

    Add Calendly script before the closing `</body>` tag (before the Supabase CDN script):

    ```html
    <!-- Calendly embed script -->
    <script type="text/javascript" src="https://assets.calendly.com/assets/external/widget.js" async></script>
    ```

    NOTE: The Calendly URL `https://calendly.com/clickresto/demo` is a placeholder. The user will need to:
    1. Create a Calendly account
    2. Create an event type called "demo"
    3. Replace the URL with their actual Calendly link
  </action>
  <verify>
    Open index.html in browser, scroll to contact section, verify Calendly widget placeholder appears (will show error until real Calendly URL configured).
  </verify>
  <done>
    - Calendly embed container added
    - Custom styling to match Clickresto design
    - Placeholder URL ready to be replaced
    - Calendly script loaded asynchronously
    - "ou" divider between form and calendar
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify and enhance mobile responsiveness</name>
  <files>
    index.html
  </files>
  <action>
    Review and enhance the existing responsive CSS to ensure all new sections work well on mobile.

    Add/update the following CSS in the `<style>` section within the responsive media queries:

    ```css
    @media (max-width: 968px) {
        /* Existing rules... */

        .testimonials-grid {
            grid-template-columns: 1fr;
        }

        .social-proof {
            gap: 2rem;
        }
    }

    @media (max-width: 768px) {
        /* Existing rules... */

        .testimonials {
            padding: 4rem 1.5rem;
        }

        .testimonial-card {
            padding: 1.5rem;
        }

        .testimonial-quote {
            font-size: 1rem;
        }

        .social-proof {
            flex-direction: column;
            gap: 1.5rem;
        }

        .proof-value {
            font-size: 2rem;
        }

        .booking-section {
            margin-top: 2rem;
            padding-top: 2rem;
        }

        .booking-section h3 {
            font-size: 1.25rem;
        }

        .calendly-inline-widget {
            height: 700px;
        }

        /* Improve mobile menu */
        .nav-links.mobile-open {
            display: flex !important;
            position: absolute;
            top: 100%;
            left: 0;
            right: 0;
            background: var(--background);
            flex-direction: column;
            padding: 1rem 2rem;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            gap: 1rem;
        }

        .nav-links.mobile-open a {
            padding: 0.75rem 0;
            border-bottom: 1px solid var(--gray-200);
        }

        .nav-links.mobile-open a:last-child {
            border-bottom: none;
        }
    }
    ```

    Update the mobile menu JavaScript to use a class toggle instead of inline styles:

    Replace the mobile menu script with:
    ```javascript
    // Mobile menu toggle (improved)
    const mobileMenuBtn = document.querySelector('.mobile-menu-btn');
    const navLinks = document.querySelector('.nav-links');

    mobileMenuBtn.addEventListener('click', () => {
        navLinks.classList.toggle('mobile-open');
        // Toggle between hamburger and X
        mobileMenuBtn.textContent = navLinks.classList.contains('mobile-open') ? '✕' : '☰';
    });

    // Close mobile menu when clicking a link
    navLinks.querySelectorAll('a').forEach(link => {
        link.addEventListener('click', () => {
            if (window.innerWidth <= 768) {
                navLinks.classList.remove('mobile-open');
                mobileMenuBtn.textContent = '☰';
            }
        });
    });
    ```
  </action>
  <verify>
    Open index.html in browser, use DevTools to test at various breakpoints (768px, 480px, 320px). All sections should display properly without horizontal scrolling.
  </verify>
  <done>
    - Testimonials section responsive at all breakpoints
    - Social proof stacks vertically on mobile
    - Calendly widget adjusts height for mobile
    - Mobile menu improved with close functionality
    - No horizontal scroll on mobile
    - Touch-friendly tap targets
  </done>
</task>

</tasks>

<verification>
1. Open index.html at desktop resolution - all sections display correctly
2. Open at 768px width - responsive layout kicks in
3. Open at 320px width - all content readable, no horizontal scroll
4. Click mobile menu - opens and closes properly
5. Navigation links scroll to correct sections
6. Testimonials section visible with 3 cards
7. Social proof numbers display
8. Calendly container visible in contact section
</verification>

<success_criteria>
- Testimonials section displays with 3 testimonial cards
- Social proof stats show (150+ restaurants, 50k+ orders, 4.8 rating)
- Navigation includes "Temoignages" link
- Calendly widget placeholder in contact section
- All sections fully responsive down to 320px
- Mobile menu toggles correctly
- French text throughout
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-lead-capture/01-04-SUMMARY.md`
</output>
